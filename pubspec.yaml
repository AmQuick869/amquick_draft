name: amquick_draft
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^4.0.4
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Correctly indented assets section
  assets:
    - assets/images/360_F_220578611_J7vb4nbxx7ZZkhVxDCSFHE1BslIgIavR-removebg-preview.png
    - assets/images/bike.png
    # If you have more images, you can add them like this:
    # - assets/images/another_image.png
    # Or include all images in the folder like this:
    - assets/images/  # Include the entire directory if you have multiple images
